package com.harmony.cmb.word.repository;

import com.harmony.cmb.word.domain.Word;
import com.harmony.umbrella.data.repository.QueryableRepository;

/**
 * @author wuxii
 */
public interface WordRepository extends QueryableRepository<Word, Long> {
}
